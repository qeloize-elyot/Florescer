services:
  - type: web
    name: florescer
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node start.js
    envVars:
      - key: NODE_VERSION
        value: "20"
